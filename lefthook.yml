pre-commit:
  commands:
    format:
      run: pnpm format
      stage_fixed: true
    lint:
      run: pnpm lint

pre-push:
  commands:
    check:
      run: pnpm check
    build:
      run: pnpm build
